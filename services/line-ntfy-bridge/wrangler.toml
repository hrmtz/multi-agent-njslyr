name = "line-ntfy-bridge"
main = "src/worker.js"
compatibility_date = "2024-01-01"

# Secrets are set via `wrangler secret set` — never hardcode them here.
# Required secrets:
#   wrangler secret set LINE_CHANNEL_SECRET
#   wrangler secret set LINE_CHANNEL_ACCESS_TOKEN
